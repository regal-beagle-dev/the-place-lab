---
# A running timer keeps its old schedule through a daemon-reload; changing
# OnCalendar only takes effect on restart.
- name: Restart the backup timer
  ansible.builtin.systemd_service:
    name: backup.timer
    state: restarted
    daemon_reload: true
